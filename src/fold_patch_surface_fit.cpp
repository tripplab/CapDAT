#include "fold_patch_surface_fit.hpp"

#include "fold_patch_analysis.hpp"

PatchReconstructedSurfaces reconstructSurfaces(const PatchContactGrid& contact_grid,
                                               const FoldPatchAnalysisConfig& config) {
    (void)contact_grid;
    (void)config;

    return {};
}
