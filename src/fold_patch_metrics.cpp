#include "fold_patch_metrics.hpp"

#include "fold_patch_analysis.hpp"

PatchMetrics computeMetrics(const PatchReconstructedSurfaces& surfaces,
                            const FoldPatchAnalysisConfig& config) {
    (void)surfaces;
    (void)config;

    return {};
}
