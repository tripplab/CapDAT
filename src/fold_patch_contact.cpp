#include "fold_patch_contact.hpp"

#include "fold_patch_analysis.hpp"

PatchContactGrid buildContactGrid(const std::vector<PatchAtom>& atoms,
                                  const FoldPatchAnalysisConfig& config) {
    (void)atoms;
    (void)config;

    return {};
}
